/**
 * X/Twitter reads twitter:image; most other messengers (iMessage, WhatsApp,
 * Slack, Discord) read og:image. Same card for both — one design, two tags.
 */
export { default, size, alt, contentType } from "./opengraph-image";
